if __name__ == '__main__':
    n = int(input())
    arr = map(int, input().split())
    arr=list(arr)
    ls=[]
    x=max(arr)
    for i in range(n):
        if x!=arr[i]:
            ls.append(arr[i])
    print(max(ls))
    
